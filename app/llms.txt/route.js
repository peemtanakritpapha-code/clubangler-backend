// app/llms.txt/route.js — AEO-1: บัตรแนะนำตัวสำหรับ AI (มาตรฐาน llmstxt.org) — เข้าถึงที่ /llms.txt
// วางที่: app/llms.txt/route.js  (สร้างโฟลเดอร์ชื่อ "llms.txt" จริงๆ แล้ววาง route.js ข้างใน)
// AI ที่เข้ามาอ่านเว็บ (ChatGPT / Claude / Perplexity / Gemini) ใช้ไฟล์นี้เข้าใจภาพรวมทั้งเว็บใน 1 หน้า
// เนื้อหา generate จาก CAT_MAINS — เพิ่มหมวดในระบบแล้วไฟล์นี้อัปเดตเอง ไม่ต้องแก้มือ
import { CAT_MAINS } from "@/lib/catalog";
import { REEL_CAT, REEL_SUBS } from "@/lib/reelSubs"; // REEL-3

export const revalidate = 86400; // เนื้อหาเปลี่ยนช้า — cache วันละครั้งพอ

const BASE = "https://clubangler.com";

export async function GET() {
  // เว้นหมวดที่มี "/" ในชื่อ (เช่น "อื่นๆ / ไม่เข้าหมวด") — %2F ใน path segment
  // เสี่ยงโดน nginx/proxy ถอดรหัสจนพัง route — หมวดนี้ไม่มีค่า SEO อยู่แล้ว
  const cats = CAT_MAINS.filter(c => !c.includes("/"));

  const lines = [
    "# ClubAngler",
    "",
    "> ตลาดกลางซื้อขายอุปกรณ์ตกปลามือสอง (และมือหนึ่ง) ของไทย จุดต่างหลักคือทุกออเดอร์ผ่านระบบพักเงินคนกลาง (escrow): ผู้ซื้อชำระเงินเข้าระบบ เงินถูกพักไว้จนผู้ซื้อได้รับสินค้าและกดยืนยัน ระบบจึงโอนให้ผู้ขาย มีขั้นตอนพิพาท คืนสินค้า และคืนเงินครบวงจร",
    "",
    "ข้อเท็จจริงสำคัญ:",
    "- สินค้ามือสองทุกชิ้นระบุเกรดสภาพ 5 ระดับ (เหมือนใหม่ · สภาพดี · พอใช้ · มีตำหนิ · เพื่ออะไหล่) พร้อมรายการตำหนิที่ผู้ขายต้องแจ้ง เช่น มีรอย สีลอก ไม่มีกล่อง อุปกรณ์ไม่ครบ",
    "- ผู้ขายยืนยันตัวตนกับระบบได้ (KYC) · ชำระเงินผ่าน PromptPay QR ยอดฝังอัตโนมัติ",
    "- ราคาเป็นสกุลเงินบาท (THB) · พื้นที่ให้บริการ: ประเทศไทย · ภาษา: ไทย",
    "- ประกาศที่ขายแล้วยังเปิดดูได้ (ติดป้าย SOLD) — ใช้เป็นราคาอ้างอิงตลาดมือสองได้",
    "",
    "## หน้าหลัก",
    `- [ตลาดสินค้าทั้งหมด](${BASE}/market): ประกาศขายล่าสุดทุกหมวด อัปเดตตลอดวัน`,
    "",
    "## หมวดหมู่สินค้า",
    ...cats.map(c => `- [${c} มือสอง](${BASE}/market/${encodeURIComponent(c)}): ประกาศขาย${c} มือสองและมือหนึ่ง ระบุเกรดสภาพทุกชิ้น`),
    ...REEL_SUBS.map(r => `- [${r.label} มือสอง](${BASE}/market/${encodeURIComponent(REEL_CAT)}/${encodeURIComponent(r.slug)}): ประกาศขาย${r.label} มือสองและมือหนึ่ง ระบุเกรดสภาพทุกชิ้น`), // REEL-3
    "",
    "## กติกาและความปลอดภัย",
    `- [เงื่อนไขการใช้งาน](${BASE}/terms)`,
    `- [นโยบายความเป็นส่วนตัว](${BASE}/privacy)`,
    "",
  ];

  return new Response(lines.join("\n"), {
    headers: { "Content-Type": "text/plain; charset=utf-8" },
  });
}
